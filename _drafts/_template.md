---
layout: post
title: Title goes here
description: One-sentence summary that shows on the index card and in social previews.
---

Drop your post here. Math via `$inline$` and `$$display$$` — backslashes,
braces, and underscores all survive (the build-time plugin protects them).

Code in fenced blocks with a language hint:

```python
def hello():
    return "world"
```

When ready to publish: rename this file to `YYYY-MM-DD-title-slug.md` and
move it into `_posts/`. The date in the filename becomes the post date.
